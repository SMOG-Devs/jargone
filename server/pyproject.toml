[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "sqlalchemy>=2.0.0",
    "fastapi[standard]",
    "pytest>=8.3.5",
    "openai>=1.73.0",
]
